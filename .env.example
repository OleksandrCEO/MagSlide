# Copy to .env and fill in. Read by Docker via --env-file (see .mcp.json).
# Raw values, no quotes, no spaces around '='. .env is gitignored — never commit it.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
